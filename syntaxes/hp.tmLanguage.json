{
  "$schema":
  "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
  "name": "HP Procurve",
  "patterns": [
    {
      "include": "#keywords"
    },
    {
      "include": "#strings"
    }
  ],
  "repository": {
    "keywords": {
      "patterns": [
        {
          "comment": "IPv4 Addresses with/without subnet",
          "name": "keyword.control.less.procurve",
          "match":
            "\\b((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))([/]\\d{1,2})?\\b"
        },
        {
          "comment": "Comments and unimportant stuffs",
          "name": "comment.procurve",
          "match": "^\\s*(module|;|conf|end|exit).*"
        },
        {
          "comment": "No and Disable",
          "name": "storage.procurve",
          "match": "^\\s*(no|disable)"
        },
        {
          "comment": "Speed-Duplex",
          "name": "entity.name.selector.procurve",
          "match": ".*speed-duplex.*"
        },
        {
          "comment": "Spanning-Tree",
          "name": "string.procurve",
          "match": ".*(spanning-tree).*"
        },
        {
          "comment": "Trunk",
          "name": "storage.modifier.procurve",
          "match": ".*trunk.*"
        },
        {
          "comment": "IGMP",
          "name": "entity.other.attribute-name.procurve",
          "match": ".*igmp.*"
        },
        {
          "comment": "DHCP Snooping",
          "name": "header.procurve",
          "match": ".*dhcp-snooping.*"
        },
        {
          "comment": "IP Authorized Managers",
          "name": "constant.procurve",
          "match": ".*ip authorized.*"
        },
        {
          "comment": "SNTP & Time",
          "name": "entity.name.selector.procurve",
          "match": ".*(sntp|time\\b).*"
        },
        {
          "comment": "SNMP",
          "name": "entity.other.attribute-name.procurve",
          "match": ".*snmp.*"
        },
        {
          "comment": "Interface",
          "name": "meta.preprocessor",
          "match": ".*(interface|int)\\b.*"
        },
        {
          "comment": "VLAN",
          "name": "markup.deleted",
          "match": ".*vlan.*"
        },
        {
          "comment": "Untagged",
          "name": "markup.changed",
          "match": "^\\s*untagged.*"
        },
        {
          "comment": "Tagged",
          "name": "keyword.control.directive",
          "match": "^\\s*tagged.*"
        },
        {
          "comment": "Loop Protection",
          "name": "keyword.control.directive",
          "match": "^\\s*loop-protect.*"
        },
        {
          "comment": "Passwords",
          "name": "punctuation.definition.logical-expression",
          "match": "^\\s*password.*"
        },
        {
          "comment": "General numbers",
          "match": "(?<=\\s)(\\d+)(?=[;]|\\s)",
          "captures": {
            "1": {
              "name": "string"
            }
          }
        }
      ]
    },
    "strings": {
      "name": "string.quoted.double.procurve",
      "begin": "\"",
      "end": "\"",
      "patterns": [
        {
          "name": "constant.character.escape.procurve",
          "match": "\\\\."
        }
      ]
    }
  },
  "scopeName": "text.procurve"
}
